package com.aura.organizationapi.app.config.security;

import org.springframework.context.annotation.Configuration;

@Configuration
//@EnableWebSecurity
public class SecurityConfig {

    //@Bean
    //public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
    //    return http
    //            .authorizeHttpRequests(authorizationRegistry -> authorizationRegistry.anyRequest().authenticated())
    //            .oauth2Login(Customizer.withDefaults())
    //            .oauth2ResourceServer(httpConfigurer -> httpConfigurer.jwt(Customizer.withDefaults()))
    //            .build();
    //}


}
